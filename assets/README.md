# Assets

This directory intentionally contains no proprietary DELTARUNE image or audio files.

`placeholders.lua` defines tiny original sprites that are converted into `ImageData` at runtime. Add legally obtained local PNGs using the paths documented in the root README. The asset loader will automatically prefer those PNGs and fall back to the placeholders when files are missing.
