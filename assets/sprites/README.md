# Sprites

Place PNG textures here. The path below this directory becomes the extensionless sprite ID.

Examples:

```text
party/kris/dark/walk/down.png     -> party/kris/dark/walk/down
ui/heart.png                      -> ui/heart
```

Use `_1.png`, `_2.png`, and so on to create ordered animation frames for one ID.
