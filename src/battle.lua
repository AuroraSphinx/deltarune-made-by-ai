-- The standalone project API stays unchanged. The implementation lives in the
-- vendored Kristal legacy port so upstream-derived code is clearly separated.
return require("vendor.kristal_legacy.battle")
